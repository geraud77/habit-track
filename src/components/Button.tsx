// type ButtonProps = {
//   text: string;
//   children: React.ReactNode;
// };
// function Button({ children }: ButtonProps) {
//   return <button>{children}</button>;
// }

// export default Button;

// // children is a prop that allows you to pass in a React node and pass it at  the middle of the button component
